"""Recorded extraction fixtures.

TASKS.md M15 requires the extractor to be tested against recorded fixtures and never against
live calls, so these are the canned model responses the suite plays back through an injected
transport.
"""
